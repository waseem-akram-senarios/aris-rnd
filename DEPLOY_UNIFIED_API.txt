========================================
DEPLOY UNIFIED API - COMPLETE UI VIA API
========================================

DEPLOYMENT COMMANDS:
--------------------

# One-line deployment:
scp unified_api_deployment.tar.gz ubuntu@44.221.84.58:/tmp/ && ssh ubuntu@44.221.84.58 'cd /tmp && tar -xzf unified_api_deployment.tar.gz && sudo cp api/unified_api.py api/main.py /home/ubuntu/aris/api/ && sudo systemctl restart aris-fastapi && echo "✅ Deployed!"'

# Step-by-step:
scp unified_api_deployment.tar.gz ubuntu@44.221.84.58:/tmp/
ssh ubuntu@44.221.84.58
cd /tmp
tar -xzf unified_api_deployment.tar.gz
sudo cp api/unified_api.py /home/ubuntu/aris/api/
sudo cp api/main.py /home/ubuntu/aris/api/
sudo systemctl restart aris-fastapi
exit

========================================
TEST AFTER DEPLOYMENT:
========================================

# 1. Get complete dashboard
curl -X GET "http://44.221.84.58:8500/ui/dashboard" | python3 -m json.tool

# 2. Get all settings
curl -X GET "http://44.221.84.58:8500/ui/settings" | python3 -m json.tool

# 3. Get document library
curl -X GET "http://44.221.84.58:8500/ui/library" | python3 -m json.tool

# 4. Get metrics
curl -X GET "http://44.221.84.58:8500/ui/metrics" | python3 -m json.tool

# 5. Get system status
curl -X GET "http://44.221.84.58:8500/ui/status" | python3 -m json.tool

# 6. Update settings (switch to Cerebras)
curl -X POST "http://44.221.84.58:8500/ui/settings" \
  -H "Content-Type: application/json" \
  -d '{"api": {"provider": "cerebras"}}' | python3 -m json.tool

========================================
WHAT'S DEPLOYED:
========================================

7 NEW ENDPOINTS:
- GET  /ui/dashboard       - Complete UI in one call
- GET  /ui/settings        - All settings
- POST /ui/settings        - Update settings
- GET  /ui/library         - Document library (8 docs)
- GET  /ui/library/{id}    - Specific document
- GET  /ui/metrics         - R&D metrics
- GET  /ui/status          - System status

COMPLETE UI FEATURES:
✅ API provider selection (OpenAI/Cerebras)
✅ Model settings (gpt-4o, embeddings, temperature)
✅ Parser settings (Docling/PyMuPDF/Textract)
✅ Chunking strategy (Comprehensive/Balanced/Fast)
✅ Vector store settings (FAISS/OpenSearch)
✅ Document library (8 documents stored)
✅ R&D metrics & analytics
✅ Storage information
✅ Processing statistics

========================================
